import { LecturerAndStudentGuard } from './lecturer-and-student-guard';

describe('LecturerAndStudentGuard', () => {
  it('should create an instance', () => {
    expect(new LecturerAndStudentGuard()).toBeTruthy();
  });
});
